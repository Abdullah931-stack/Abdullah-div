# 🎨 UI/UX Design System & Guidelines
## Advanced Personal Page — v1.3

---

## 1. الفلسفة التصميمية (Design Philosophy)

> **"الفخامة والهدوء"** — هذا هو المبدأ الحاكم للتصميم بأكمله.
> المصدر: `صفحتي الشخصية (v 1.1).md` — القسم 1

كل قرار تصميمي يجب أن يمر عبر هذا الفلتر:
- هل يُعزز الشعور بالفخامة؟ ✅
- هل يحافظ على الهدوء البصري؟ ✅
- هل يُضيف ضوضاءً بصرياً غير ضرورية؟ ❌

---

## 2. نظام السمات — Dark / Light Mode

> [!IMPORTANT]
> الموقع **داكن بشكل افتراضي وأساسي** مع إتاحة التبديل للوضع الفاتح (Light) يدوياً.

### لوحة ألوان الوضع الداكن (Dark — Default)

| الدور | اللون | الاستخدام |
|---|---|---|
| **Primary** | أخضر خافت (Muted Green) `≈ hsl(150, 25%, 35%)` | العناصر التفاعلية، الأزرار، الروابط النشطة |
| **Background** | درجات غامقة (Deep Dark) `≈ hsl(200, 15%, 8%)` | خلفية الصفحات |
| **Surface** | سطح شفاف مموّه `rgba(255,255,255,0.05) + backdrop-filter: blur()` | البطاقات، الأقسام |
| **Text Primary** | أبيض مائل للدفء `≈ hsl(40, 10%, 92%)` | النصوص الرئيسية |
| **Text Secondary** | رمادي فاتح `≈ hsl(200, 10%, 60%)` | النصوص الثانوية |
| **Accent** | مشتق من الأخضر الخافت (أكثر إشراقاً) | تأثيرات Hover، التمييز |

### لوحة ألوان الوضع الفاتح (Light — Optional)

| الدور | اللون | الاستخدام |
|---|---|---|
| **Primary** | أخضر خافت (أغمق قليلاً) `≈ hsl(150, 30%, 30%)` | نفس الاستخدام |
| **Background** | أبيض دافئ `≈ hsl(40, 20%, 96%)` | خلفية الصفحات |
| **Surface** | أبيض مع ظل خفيف `rgba(0,0,0,0.03) + backdrop-filter: blur()` | البطاقات |
| **Text Primary** | رمادي غامق جداً `≈ hsl(200, 15%, 15%)` | النصوص الرئيسية |
| **Text Secondary** | رمادي متوسط `≈ hsl(200, 10%, 45%)` | النصوص الثانوية |
| **Accent** | نفس الأخضر الخافت | تأثيرات Hover |

### التنفيذ التقني

```css
/* CSS Custom Properties */
:root, [data-theme="dark"] {
    --color-primary: hsl(150, 25%, 35%);
    --color-bg: hsl(200, 15%, 8%);
    --color-surface: rgba(255, 255, 255, 0.05);
    --color-text: hsl(40, 10%, 92%);
    --color-text-secondary: hsl(200, 10%, 60%);
}

[data-theme="light"] {
    --color-primary: hsl(150, 30%, 30%);
    --color-bg: hsl(40, 20%, 96%);
    --color-surface: rgba(0, 0, 0, 0.03);
    --color-text: hsl(200, 15%, 15%);
    --color-text-secondary: hsl(200, 10%, 45%);
}
```

### قواعد استخدام الألوان

1. **تأثير التمويه (Blur)** إلزامي على خلفيات البطاقات والأسطح في كلا الوضعين
2. الألوان يجب أن "تندمج مع الخلفية" — لا حواف حادة أو تباين صارخ
3. نسبة التباين (Contrast Ratio) يجب أن تحقق معايير **WCAG AA** كحد أدنى في كلا الوضعين
4. تخزين تفضيل المستخدم في `localStorage`

---

## 3. التصميم المتجاوب (Responsive Design)

> [!IMPORTANT]
> المتطلب ينص صراحةً على أن يكون هناك **"تصميم مخصص ومتجاوب لكل منهما منفصلاً عن الآخر"** — أي ليس مجرد تكييف (adaptation) بل **تصميم مستقل** لكل من الـ PC والهواتف.

### استراتيجية التصميم

| المنصة | النهج | ملاحظات |
|---|---|---|
| **Desktop (≥ 1024px)** | تصميم أساسي كامل | تأثيرات Hover، تفاعل الماوس، 2.5D Parallax |
| **Mobile (< 768px)** | تصميم مستقل بالكامل | Touch interactions، تبسيط الحركات، تحسين الأداء |
| **Tablet (768px – 1023px)** | تصميم هجين | تكييف ذكي بين الاثنين |

### قواعد لكل منصة

**Desktop:**
- بطاقة "من أنا" + الشخصية 2.5D بتأثير Parallax الكامل
- معرض المشاريع: ملخص يمين + صور يسار (تخطيط أفقي — RTL)
- السجل الزمني: بطاقات متتابعة مع ملخص على اليمين

**Mobile:**
- تبسيط تأثير 2.5D → تفاعل اللمس (Touch Tilt) أو إلغاؤه لصالح الأداء
- معرض المشاريع: تخطيط عمودي (Card Stack)
- السجل الزمني: تخطيط عمودي بالكامل
- الاستبيان: بطاقة واحدة كاملة العرض لكل سؤال

---

## 4. التخطيط العام (Layout System)

### الشبكة (Grid)

```
Desktop:  12-column grid, max-width: 1280px, gap: 24px
Tablet:   8-column grid, padding: 24px
Mobile:   4-column grid, padding: 16px
```

### المسافات (Spacing Scale)

```
--space-xs:   4px
--space-sm:   8px
--space-md:   16px
--space-lg:   24px
--space-xl:   32px
--space-2xl:  48px
--space-3xl:  64px
--space-4xl:  96px
```

---

## 5. الخطوط (Typography)

> [!IMPORTANT]
> الخطوط **مؤكدة** من صاحب المشروع:
> - **العربية:** Readex Pro
> - **الإنجليزية:** Plus Jakarta Sans

| المستوى | الخط العربي | الخط الإنجليزي | الحجم (Desktop) | الحجم (Mobile) |
|---|---|---|---|---|
| **Display / Hero** | Readex Pro | Plus Jakarta Sans | 48–64px | 28–36px |
| **Heading 1** | Readex Pro | Plus Jakarta Sans | 36–40px | 24–28px |
| **Heading 2** | Readex Pro | Plus Jakarta Sans | 28–32px | 20–24px |
| **Body** | Readex Pro | Plus Jakarta Sans | 16–18px | 14–16px |
| **Caption / Small** | Readex Pro | Plus Jakarta Sans | 12–14px | 12px |

### القواعد

- **اتجاه النص:** RTL للعربية، LTR للإنجليزية — يتغير ديناميكياً مع تبديل اللغة
- **ارتفاع السطر:** `1.6` للنصوص، `1.2` للعناوين
- **وزن الخط:** Regular (400) للنصوص، Bold (700) للعناوين
- **تحميل الخطوط:** عبر `next/font/google` — محلي لتحسين الأداء

### التنفيذ

```tsx
// next/font/google
import { Readex_Pro, Plus_Jakarta_Sans } from 'next/font/google';

const readexPro = Readex_Pro({
  subsets: ['arabic', 'latin'],
  variable: '--font-ar',
  display: 'swap',               // 🆕 لمنع اختفاء النص (FOIT)
});

const plusJakartaSans = Plus_Jakarta_Sans({
  subsets: ['latin'],
  variable: '--font-en',
  display: 'swap',               // 🆕 لمنع اختفاء النص (FOIT)
});
```

> [!TIP]
> `display: 'swap'` يعرض النص بخط النظام فوراً ثم يستبدله عند تحميل الخط المخصص. هذا يمنع **FOIT (Flash of Invisible Text)** خاصةً أن الخطوط العربية قد تكون ثقيلة قليلاً.

---

## 6. مكونات التصميم (Component Tokens)

### البطاقات (Cards)

```css
.card {
    background: var(--color-surface);
    backdrop-filter: blur(16px);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 16px;
    padding: var(--space-lg);
    transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.card:hover {
    transform: translateY(-4px);
    box-shadow: 0 16px 48px rgba(0, 0, 0, 0.3);
}
```

### نموذج التواصل الذكي — Glassmorphism Card

```css
.smart-contact-card {
    background: rgba(255, 255, 255, 0.06);
    backdrop-filter: blur(24px);
    -webkit-backdrop-filter: blur(24px);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 20px;
    padding: var(--space-2xl);
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.25);
}
```

### الأزرار (Buttons)

```css
.btn-primary {
    background: var(--color-primary);
    color: var(--color-text);
    border: none;
    border-radius: 12px;
    padding: 12px 24px;
    font-weight: 600;
    transition: all 0.2s ease;
}

.btn-primary:hover {
    filter: brightness(1.15);
    transform: translateY(-2px);
}

/* زر "ابدأ المشروع 🚀" */
.btn-submit-project {
    background: linear-gradient(135deg, var(--color-primary), var(--color-accent));
    font-size: 1.1rem;
    padding: 14px 32px;
    border-radius: 14px;
}
```

### الـ Footer

```
- يظل ثابتاً في أسفل الصفحة
- يحتوي على روابط: واتساب، لينكد إن، مستقل
- بنية مرنة (Array-based) لإضافة وسائل تواصل مستقبلاً
- يحتوي على: Language Switcher + Theme Switcher
- تصميم بسيط لا يطغى على المحتوى الأساسي
```

---

## 7. تصنيف كثافة الحركة حسب الصفحة (Animation Density Map)

> المصدر: *"حركات بسيطة في الأقسام العامة، وتأثيرات حركية شديدة الاحترافية في الصفحات الرئيسية"*

| الصفحة / القسم | مستوى الكثافة | الأمثلة |
|---|---|---|
| Footer / Navbar | 🟢 **منخفض** | Fade-in بسيط، hover effects |
| الصفحة الرئيسية | 🔴 **عالي جداً** | 2.5D Parallax، Card Shuffle، طفو النص |
| رحلتي (Timeline) | 🔴 **عالي جداً** | Scroll-triggered animations، بطاقات متتابعة |
| معرض المشاريع | 🔴 **عالي جداً** | Image transitions، Fullscreen zoom |
| التواصل والاستبيان | 🔴 **عالي جداً** | بطاقات احترافية، Glassmorphism، Modal احتفالي |
| لوحة التحكم | 🟢 **منخفض** | وظيفية أولاً، حركات بسيطة |
